{
  "name": "wp_final",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/walkerhsu/wp_final.git",
  "author": "walkerhsu <yp10731155@gmail.com>",
  "license": "MIT",
  "scripts": {
    "frontend": "cd frontend && yarn run start",
    "backend": "cd backend && yarn run server",
    "install:all": "cd frontend && yarn install && cd ../backend && yarn install",
    "install:prod": "cd frontend && yarn install --freeze-lockfile && cd ../backend && yarn install --freeze-lockfile",
    "build": "cd frontend && yarn build",
    "deploy": "cd backend && yarn deploy"
  },
  "devDependencies": {
    "prettier": "^2.8.1"
  }
}
